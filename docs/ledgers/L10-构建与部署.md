# L10 · 构建与部署

> 上级：[总台账](../总台账.md)　|　契约：[contract.md](../contract.md) C-8 / C-10　|　状态：🟡 进行中

## 目标
保证本地构建可复现、零错误；改动经确认后安全上线。

## 本地构建

| 项 | 命令 / 值 |
|---|---|
| 构建命令 | `npm run build`（Astro） |
| 输出目录 | `dist/` |
| 部署 | `git push origin main` → Vercel 自动构建 |
| 本地预览 | `npm run dev` |

## 已完成
- [x] Astro 构建通过（69 页）
- [x] 各批次改动均已 commit 为 checkpoint（防 safe-delete / 同步工具破坏）
- [x] 审计脚本 `scripts/audit.py` 建立（可复现的质量门禁）

## 待办
- [ ] **所有改动尚未 push，线上仍是旧版** —— 用户确认后执行 `git push origin main`
- [ ] push 后核对：4 条 301 生效、/about/holive-hu 等新页可访问、GSC 无新增 404
- [ ] 建议把 `scripts/audit.py` 接入提交前检查（质量门禁自动化）

## 构建环境注意（踩过的坑）
- Windows 上 `hugo --gc`（旧栈）曾因 `public` 目录文件锁定报 "Access is denied" → 清 `public/` 用 `mv` 移走再构建
- 沙箱 safe-delete 会拦截目录删除并可能级联吞目录 → 契约 C-8

## 验收口径
- 构建零 error
- `scripts/audit.py` 指标不回退
- push 后线上随机抽查 3 个新页 + 1 个 301
